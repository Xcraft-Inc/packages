export TMP=/tmp
export TEMP=/tmp
export TMPDIR=/tmp
export TERM=linux

if [ -d /etc/profile.d ]; then
  for i in /etc/profile.d/*.sh; do
    if [ -r "$i" ]; then
      . "$i"
    fi
  done
  unset i
fi
