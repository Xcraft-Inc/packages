# Basic mdev.conf configuration
# Device            User:Group    Perms   Action

# Null and zero devices  
null                root:root     666
zero                root:root     666
random              root:root     444
urandom             root:root     444

tty                 root:root     666

snd/pcmC[0-9]D[0-9][cp] root:root 666
snd/timer               root:root 666
snd/seq                 root:root 666
snd/controlC[0-9]*      root:root 666     *which curl >/dev/null 2>&1 \
  && curl -X POST -H "Accept: application/json" "http://[::]:9000/whispi/reload-audio"
